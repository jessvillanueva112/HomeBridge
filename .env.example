# Flask Configuration
# The main application file
FLASK_APP=app.py
# Set to 'development' for local development, 'production' for deployment
FLASK_ENV=development
# Enable detailed error messages (1) or disable them (0)
FLASK_DEBUG=1

# Security
# Generate a secure random key using: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=your_session_secret_here

# Database Configuration
# For local development, you can use the default SQLite database
# For production, use PostgreSQL with this format:
# postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://user:password@localhost:5432/homebridge

# Optional: Error Tracking
# Get your DSN from Sentry (https://sentry.io) if you want error tracking
SENTRY_DSN=your_sentry_dsn_here 